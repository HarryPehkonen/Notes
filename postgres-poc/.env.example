# PostgreSQL Database Configuration

# Database connection settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=notes_app
DB_USER=notes_user
DB_PASSWORD=your_secure_password

# Optional: Connection pool settings
DB_POOL_SIZE=3

# For testing purposes - set this to true to enable detailed logging
DEBUG_SQL=false
